# Neat_Crafting 组件关系可视化图

## 📐 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    NC_HandcraftWindow                        │
│                    (主制作窗口)                                │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                  NC_CraftHeader                          │ │
│  │                  (标题栏 + 搜索)                           │ │
│  │  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐ │ │
│  │  │NC_SearchBox │ │ NC_Button   │ │   NC_SquareButton   │ │ │
│  │  │(搜索框)      │ │(调试/关闭)  │ │    (关闭按钮)       │ │ │
│  │  └─────────────┘ └─────────────┘ └─────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────┘ │
│                                                             │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                NC_HandCraftPanel                         │ │
│  │                   (主制作面板)                              │ │
│  ├─────────────────────────────────────────────────────────┤ │
│  │ ┌─────────┐ ┌─────────────┐ ┌─────────────────────────┐ │ │
│  │ │Category │ │ RecipeList  │ │      InputSwitch          │ │ │
│  │ │  List   │ │   Panel     │ │        Panel              │ │ │
│  │ ├─────────┤ ├─────────────┤ ├─────────────────────────┤ │ │
│  │ │NC_Cate- │ │NC_Recipe-   │ │NC_InputSwitch_ + Expanded │ │ │
│  │ │goryList │ │List_Box/    │ │NC_InputSwitch_Box        │ │ │
│  │ │_Panel  │ │Grid         │ │                           │ │ │
│  │ └─────────┘ └─────────────┘ └─────────────────────────┘ │ │
│  │                                                             │ │
│  │ ┌─────────────────────┐ ┌─────────────────────────────┐ │ │
│  │ │   NC_CraftActionPanel│ │    NC_CraftInput_Panel     │ │ │
│  │ │    (制作操作控制)      │ │      (材料显示)            │ │ │
│  └─────────────────────────┴─────────────────────────────┘ │ │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │              NC_CraftOutput_Panel                        │ │
│  │                 (制作结果显示)                             │ │
│  └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 🏗️ 继承关系层次图

```
ISPanel (基础面板)
├── NC_EntityWindow
│   └── NC_CraftHeader
├── NC_HandcraftWindow
├── NC_CategoryList_Panel
│   └── NC_CategoryList_Slot (ISUIElement)
├── NC_SearchBox
├── NC_FilterBar
├── NC_CraftActionPanel
├── NC_CraftInput_Panel
├── NC_CraftOutput_Panel
├── NC_RecipeList_Panel
├── NC_InputSwitch_Panel
├── NC_CellBaseCraft
│   ├── NC_CellRecipeList
│   └── NC_CellCraftResult
├── NC_CategoryText_Panel
└── CraftLogic Components
    ├── NC_CraftLogicPanel
    └── NC_CraftLogicInventoryPanel

ISTableLayout (表格布局)
├── NC_HandCraftPanel
└── NC_CraftHeader

ISUIElement (UI元素基础)
├── NC_RecipeList_Box
├── NC_RecipeList_Grid
├── NC_CraftOutput_Slot
├── NC_CraftInput_Slot
└── NC_InputSwitch_Box/Expanded

ISButton (按钮组件)
└── NC_SquareButton
```

## 🔄 数据流向图

```
用户操作
    ↓
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Category       │    │    Recipe        │    │     Input        │
│   Selection      │───▶│   Filtering      │───▶│   Management     │
│                 │    │                 │    │                 │
│ onCategoryChanged│    │ updateRecipeList │    │updateInputPanel │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  Recipe Display  │    │  Material Update │    │  Craft Actions   │
│                 │    │                 │    │                 │
│ updateRecipeInfo│    │updateCraftInputs │    │updateCraftState │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 ▼
                    ┌─────────────────┐
                    │  Output Update  │
                    │                 │
                    │updateCraftOutput│
                    └─────────────────┘
```

## 📱 布局结构图

```
NC_HandcraftWindow Layout:
┌─────────────────────────────────────────────┐
│ Header Area (headerHeight)                    │
│ [搜索框] [按钮组] [关闭按钮]                   │
├─────────────────────────────────────────────┤
│ Main Content Area                             │
│ ┌─────────┬─────────────────┬─────────────────┐ │
│ │Category │   Recipe List    │   Input Panel    │ │
│ │ Panel  │                 │                 │ │
│ │(固定宽度)│   (剩余宽度)      │   (固定宽度)      │ │
│ ├─────────┼─────────────────┼─────────────────┤ │
│ │         │   Recipe Item    │   Input Item     │ │
│ │         │   Recipe Item    │   Input Item     │ │
│ │         │   Recipe Item    │   Input Item     │ │
│ │         │      ...         │      ...         │ │
│ └─────────┴─────────────────┴─────────────────┘ │
├─────────────────────────────────────────────┤
│ Action Area                                   │
│ [制作按钮] [数量控制] [最大数量] [经验显示]     │
├─────────────────────────────────────────────┤
│ Output Area                                   │
│ [制作结果物品显示区域]                         │
└─────────────────────────────────────────────┘
```

## 🎯 组件职责矩阵

| 组件 | 主要职责 | 依赖组件 | 交互事件 |
|------|----------|----------|----------|
| **NC_HandcraftWindow** | 窗口容器、生命周期管理 | 所有子组件 | onGameStart, onClose |
| **NC_CraftHeader** | 标题显示、搜索功能 | NC_SearchBox | onSearchChange |
| **NC_CategoryList_Panel** | 分类选择管理 | NC_CategoryList_Slot | onCategoryChanged |
| **NC_RecipeList_Panel** | 配方显示和选择 | NC_RecipeList_Box/Grid | onRecipeSelected |
| **NC_InputSwitch_Panel** | 材料输入管理 | NC_InputSwitch_Box | onItemSwitch |
| **NC_CraftActionPanel** | 制作操作控制 | NC_SquareButton, ISButton | onCraftStart |
| **NC_CraftInput_Panel** | 材料状态显示 | NC_CraftInput_Slot | onMaterialUpdate |
| **NC_CraftOutput_Panel** | 结果显示 | NC_CraftOutput_Slot | onCraftComplete |

## 🔗 事件通信图

```
事件流:
User Input → Component → Logic Layer → UI Update

具体事件链:
1. Category Selection
   NC_CategoryList_Slot.onClick
   → NC_CategoryList_Panel.onCategoryChanged
   → NC_HandCraftPanel.onCategoryChanged
   → NC_RecipeList_Panel.updateRecipeList

2. Recipe Selection
   NC_RecipeList_Box.onClick
   → NC_RecipeList_Panel.onRecipeSelected
   → NC_HandCraftPanel.onRecipeSelected
   → NC_InputSwitch_Panel.updateForRecipe

3. Craft Action
   NC_CraftActionPanel.onCraftButtonClick
   → NC_HandCraftPanel.onCraftStart
   → NC_CraftLogicPanel.performCraft
   → NC_CraftOutput_Panel.updateOutput
```

## 🎨 样式系统图

```
NeatUI_Framework 依赖:
┌─────────────────┐
│ 9-Patch System │
├─────────────────┤
│ CategoryBG      │ ← 分类面板背景
│ ContentPanel    │ ← 内容面板背景
│ Button          │ ← 按钮样式
│ ScrollBar       │ ← 滚动条样式
└─────────────────┘

Icon System:
┌─────────────────┐
│ Category Icons   │
├─────────────────┤
│ *.png files     │ ← 各类制作分类图标
│ Cooking         │
│ Blacksmithing   │
│ Carpentry       │
│ ...             │
└─────────────────┘

Color Scheme:
┌─────────────────┐
│ Primary Colors  │
├─────────────────┤
│ Background      │ ← 面板背景色
│ Border          │ ← 边框颜色
│ Text            │ ← 文本颜色
│ Highlight       │ ← 高亮颜色
└─────────────────┘
```

这个可视化图清晰地展示了 Neat_Crafting 的组件架构、数据流向和交互关系，为开发者提供了直观的参考。